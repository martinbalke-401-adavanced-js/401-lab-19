import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

//Render app to the page at the root element
ReactDOM.render(<App />, document.getElementById('root'));
